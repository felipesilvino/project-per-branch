apply from: "${rootDir}/gradle/spring-boot.gradle"
apply from: "${rootDir}/gradle/service-discovery.gradle"
apply from: "${rootDir}/gradle/service-monitoring.gradle"

dependencies {
    compile project (":person-core")
    runtime (database.postgre) {}
}
